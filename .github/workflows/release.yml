name: Build and publish
on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:
    inputs:
      publish:
        description: Publish the verified artifacts to PyPI
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          version: '0.11.19'
          python-version: '3.12'

      - name: Build wheel from source archive
        run: python tools/build.py
      - name: Verify release artifacts
        run: |
          python tools/verify.py dist
          uvx --from twine==7.0.0 twine check --strict dist/*.whl dist/*.tar.gz
      - name: Test isolated wheel installation
        run: |
          uv venv "$RUNNER_TEMP/wheel-test" --python 3.12
          uv pip install --python "$RUNNER_TEMP/wheel-test/bin/python" dist/*.whl

          "$RUNNER_TEMP/wheel-test/bin/python" -I tools/smoke.py
      - uses: actions/upload-artifact@v4
        with:
          name: release
          path: |
            dist/*.whl
            dist/*.tar.gz
            dist/build.json
            dist/auditwheel.txt
  publish:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && inputs.publish)
    runs-on: ubuntu-22.04
    environment: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: release
          path: dist
      - uses: astral-sh/setup-uv@v6
        with:
          version: '0.11.19'
      - name: Publish to PyPI
        run: uv publish --trusted-publishing always --check-url https://pypi.org/simple dist/*.whl dist/*.tar.gz
